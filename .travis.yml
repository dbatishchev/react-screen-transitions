language: node_js
node_js:
  - "stable"
cache:
  directories:
    - node_modules
script:
  - npm run build

deploy:
  provider: netlify
  site: $site_id
  auth: $netlify_token
  cleanup: true
  dir: build
  prod: true
  on:
    branch: main
